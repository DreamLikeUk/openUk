<div class="col-md-4">
    <div>
        <img src="<%= category.link %>" alt="<%= category.name %>"width="250" height="250" class="img-circle img-thumbnail">
        <h2><%= category.name %> </h2>
        <p><%= category.description %></p>
        <a href="#" class="btn btn-primary" title="See more"><%= messages.category.button %></a>
    </div>
</div>